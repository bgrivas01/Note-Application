# Note-Application

Back End run Requirement: Flask-cors
to install -> python -> pip install flask flask-cors
if there is problem when install on windows, install python first, then "python -m pip install flask flask-cors" in cmd